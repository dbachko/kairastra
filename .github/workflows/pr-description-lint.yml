name: pr-description-lint

on:
  pull_request:
    types: [opened, edited, reopened, synchronize, ready_for_review]

jobs:
  validate-pr-description:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate PR description format
        env:
          PR_BODY_JSON: ${{ toJson(github.event.pull_request.body) }}
        run: |
          set -euo pipefail
          printf '%s' "$PR_BODY_JSON" | jq -r '.' > /tmp/pr_body.md

          required_headers=(
            "#### Context"
            "#### TL;DR"
            "#### Summary"
            "#### Alternatives"
            "#### Test Plan"
          )

          for header in "${required_headers[@]}"; do
            if ! rg -Fq "$header" /tmp/pr_body.md; then
              echo "Missing required PR section: $header" >&2
              exit 1
            fi
          done

          if rg -n '<!--|TODO|TBD' /tmp/pr_body.md; then
            echo "PR description still contains placeholders" >&2
            exit 1
          fi
